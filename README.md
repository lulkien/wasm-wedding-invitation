# 💍 Wedding Invitation

A personalised, fullstack wedding invitation web app built with [Dioxus 0.7](https://dioxuslabs.com/learn/0.7). Each guest receives a unique link that greets them by name and lets them RSVP with their shuttle departure preference.

---

## Features

- **Personalised invitations** — each guest link (`/invitation/:uid`) shows a custom greeting
- **Live countdown** — ticks down to the ceremony in days, hours, minutes, seconds
- **RSVP** — guests select a shuttle departure point; the choice is persisted to a SQLite database
- **Zalo group links** — QR codes and links appear based on the chosen shuttle stop
- **Fully static config** — ceremony details, venue, and couple names live in a single JSON file; no code changes needed
- **SCSS pipeline** — styles are authored in SCSS and compiled to CSS at build time via `build.rs`

---

## Tech Stack

| Layer | Technology |
|---|---|
| UI framework | [Dioxus 0.7](https://dioxuslabs.com) (fullstack, WASM) |
| Language | Rust |
| Styling | SCSS → CSS via [`sass-rs`](https://crates.io/crates/sass-rs) |
| Database | SQLite via [`rusqlite`](https://crates.io/crates/rusqlite) (bundled) |
| Routing | Dioxus Router |
| Async timers | [`gloo-timers`](https://crates.io/crates/gloo-timers) |
| Browser open | [`webbrowser`](https://crates.io/crates/webbrowser) |

---

## Project Structure

```
wasm-wedding-invitation/
├── assets/
│   ├── scss/               ← SCSS source (edit these)
│   │   ├── _variables.scss
│   │   ├── _reset.scss
│   │   ├── _utilities.scss
│   │   ├── _components.scss
│   │   ├── _overlays.scss
│   │   ├── _hero.scss
│   │   ├── _navigation.scss
│   │   ├── _sections.scss
│   │   └── main.scss
│   ├── main.css            ← generated by build.rs (do not edit)
│   └── *.webp / *.jpg      ← image assets
├── config/
│   ├── wedding.json        ← ceremony, venue & couple config
│   └── README.md
├── src/
│   ├── components/
│   │   ├── hero.rs
│   │   ├── navigation_bar.rs
│   │   ├── common.rs
│   │   └── main_content/
│   │       ├── introduce_section.rs
│   │       ├── time_section.rs
│   │       ├── location_section.rs
│   │       └── rsvp_section.rs
│   ├── pages/
│   │   ├── invitation.rs
│   │   └── page_not_found.rs
│   ├── database/
│   │   └── mod.rs
│   ├── config.rs
│   └── main.rs
├── build.rs                ← compiles SCSS → assets/main.css
├── Cargo.toml
└── Dioxus.toml
```

---

## Configuration

Edit `config/wedding.json` to customise all ceremony details — no code changes required.

```json
{
  "ceremony": {
    "date_utc": "2026-03-28T06:30:00Z",
    "date_display": "Saturday, March 28, 2026",
    "reception_time": "8:30 AM",
    "ceremony_time": "1:30 PM"
  },
  "venue": {
    "name": "Near Doc Suoi Roundabout",
    "address": "Trieu Tien Village, Son Nam Ward, Hung Yen Province",
    "maps_embed_url": "YOUR_GOOGLE_MAPS_EMBED_URL",
    "maps_directions_url": "YOUR_GOOGLE_MAPS_LINK"
  },
  "couple": {
    "groom_name": "Luu Hoang Kien",
    "bride_name": "Pham Thi Hang",
    "title": "Hoang Kien & Pham Hang"
  }
}
```

See [`config/README.md`](config/README.md) for a full field reference.

---

## Database

Guest data is stored in a SQLite database with three tables:

```sql
CREATE TABLE people (
    uid     TEXT NOT NULL UNIQUE PRIMARY KEY,
    greeting TEXT NOT NULL,
    name    TEXT NOT NULL,
    line1   TEXT NOT NULL,
    line2   TEXT,
    line3   TEXT,
    desc    TEXT
);

CREATE TABLE location_map (
    location_id  INTEGER NOT NULL UNIQUE PRIMARY KEY,
    location_name TEXT NOT NULL
);

CREATE TABLE location (
    uid TEXT NOT NULL UNIQUE PRIMARY KEY,
    depart_from INTEGER
);
```

Each guest's unique invitation URL is `/invitation/<uid>` where `uid` is the 8-character hex key from the `people` table.

---

## Quick Start

See [DEVELOP.md](DEVELOP.md) for the full development guide.

```bash
# Install the Dioxus CLI
curl -sSL http://dioxus.dev/install.sh | sh

# Run in development mode
dx serve
```

---

## License

[Unlicense](LICENSE) - Public domain dedication

This is free and unencumbered software released into the public domain.
Anyone is free to copy, modify, publish, use, compile, sell, or
distribute this software, either in source code form or as a compiled
binary, for any purpose, commercial or non-commercial, and by any
means.

For more information, please refer to <https://unlicense.org>